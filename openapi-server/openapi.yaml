# DO NOT EDIT
# Auto generated by "flask gen"

info:
  description: Demo project for OpenAPI workflow.
  title: Oasis
  version: 0.1.0
paths:
  /api/post/list:
    get:
      summary: Get post list.
      tags:
      - post
      x-swagger-router-controller: oasis.views
      operationId: get_post_list
      parameters:
      - in: query
        name: page
        schema:
          type: integer
          minimum: 1
      - in: query
        name: sort
        schema:
          type: string
          enum:
          - asc
          - desc
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  posts:
                    type: array
                    items:
                      $ref: '#/components/schemas/Post'
openapi: 3.0.2
components:
  schemas:
    Post:
      type: object
      properties:
        id:
          type: integer
        updated_at:
          type: string
          format: date-time
        content:
          type: string
          minLength: 1
        title:
          type: string
          minLength: 1
      required:
      - content
      - title
